<?php 


	if (!isset($_POST['name'])
		|| !isset($_POST['age'])
		|| !isset($_POST['password'])
		|| !isset($_POST['email'])

	) {
		header('Location: index.php');
	}

	$name = htmlspecialchars($_POST['name']);
	$age = (int) $_POST['age'];

	if ($age < 18) {
		$message = 'Not allows';
	} else {
		$message = 'Good';
	}



?>

<!DOCTYPE html>
<html>
<head>
	<title></title>
</head>
<body>
	<?= $message ?>
</body>
</html>
